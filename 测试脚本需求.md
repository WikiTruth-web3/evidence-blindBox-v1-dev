# 需求文档

## 当前
项目中有两个合约版本，
1. `contracts-EVM/`: 可以被用于以太坊的EVM网络，
2. `contracts/`: 可以被用于oasis sapphire （兼容EVM）隐私网络。

3. `test-EVM/`: 当前以太坊的EVM版本的测试脚本已经完成编写（JavaScript），并且全部通过测试。

## 这里要说明两个版本的合约之间的不同点：

### SiweAuth合约
该合约是用于SIWE认证的合约，用于在sapphire网络中验证msgSender的身份，被TruthBox、UserId等合约使用。
1. `contracts-EVM/`:合约中将SiweAuth相关合约修改为“.txt”， 因为本地以太坊测试环境，不需要这个合约，。
2. `contracts/`: 合约中需要SiweAuth相关合约。


### Sapphire库
这个库是oasis sapphire隐私网络，用来加密和解密数据的库，用于在sapphire网络中加密和解密数据，
被TruthBox、SiweAuth等合约使用。
1. `contracts-EVM/`:合约中将Sapphire库注释掉了，因为本地以太坊测试环境，不需要这个库。
2. `contracts/`: 合约中需要Sapphire库，实现加密存储，加密解密功能。

当然由于Sapphire库并未进行显式调用，所以不需要在测试中编写测试用例。这里只是为了说明，在sapphire网络中，需要使用Sapphire库。

## 需求
我们现在需要编写oasis sapphier隐私网络上的合约的测试脚本。

1. **脚本语言**：Typescript
2. **测试用例**：可以参考`test-EVM/`中的测试用例，但是由于它是使用JavaScript编写，另外还可以将其变得更加模块化一点。
3. **SiweAuth合约**：应该要增加Siwe令牌的测试，可以参考`test/single/SiweAuth.ts`中对Siwe令牌的测试方法。
4. **功能模块**：`test/utils/`这个路径下有相关的功能函数组件。

## 备注
当前的EVM版本的测试脚本测试用例是非常完善的，只是它是我个人编写的，显得不太专业和模块化，所以需要在实现新的测试脚本时，可以参照测试用例，用更加专业的方式实现新的测试脚本。

----------------------------------

## 详细需求分析

### 1. 项目架构对比分析

#### 1.1 合约版本差异
- **contracts-EVM/**: 以太坊EVM兼容版本，已移除SiweAuth和Sapphire库依赖
- **contracts/**: Oasis Sapphire隐私网络版本，包含完整的SiweAuth认证和Sapphire加密功能

#### 1.2 核心差异点
1. **SiweAuth合约**：
   - EVM版本：`SiweAuth.txt`（注释状态）
   - Sapphire版本：`SiweAuth.sol`（完整功能）
   
2. **Sapphire库**：
   - EVM版本：注释掉所有Sapphire相关导入和调用
   - Sapphire版本：完整集成Sapphire加密/解密功能

3. **隐私功能**：
   - EVM版本：无隐私保护，数据明文存储
   - Sapphire版本：支持数据加密存储和隐私保护

### 2. 测试脚本开发需求

#### 2.1 技术栈要求
- **语言**: TypeScript（与现有test/utils/保持一致）
- **测试框架**: Hardhat + Chai + Mocha
- **网络环境**: Oasis Sapphire测试网/本地网络
- **认证方式**: SIWE令牌认证（Sign-In with Ethereum）

#### 2.2 测试覆盖范围

##### 2.2.1 核心合约测试
1. **TruthBox.sol** - 真理盒子合约
   - 基本铸造功能（包含SIWE认证）
   - 黑名单管理
   - 代币转移和权限控制
   - 延迟deadline机制
   - 公开操作功能
   - Sapphire加密存储验证

2. **Exchange.sol** - 交易合约
   - 订单创建和管理
   - 买卖交易流程
   - 价格和状态管理
   - SIWE认证集成

3. **FundManager.sol** - 资金管理合约
   - 多代币支持
   - 奖励分配机制
   - 提款功能
   - 滑点保护
   - 延期费管理

4. **TruthNFT.sol** - NFT合约
   - NFT铸造和转移
   - 元数据管理
   - 权限控制

5. **UserId.sol** - 用户身份合约
   - 用户ID管理
   - Sapphire加密存储
   - 身份验证

6. **AddressManager.sol** - 地址管理合约
   - 合约地址管理
   - 权限控制
   - 地址更新机制

##### 2.2.2 SiweAuth认证测试
实际上已经完成！请查看`test/single/SiweAuth.ts`，只是没有实现整个项目的集成测试。

1. **多域名支持**
   - 主域名和子域名登录
   - 域名有效性验证
   - 域名管理（添加/移除）

2. **令牌管理**
   - SIWE令牌生成和验证
   - 令牌撤销机制
   - 跨域名令牌特性

3. **权限控制**
   - 管理员权限管理
   - 认证状态检查
   - 资源访问控制

##### 2.2.3 Sapphire隐私功能测试
1. **加密存储验证**
   - 数据加密存储测试
   - 加密数据解密验证
   - 隐私保护机制

2. **密钥管理**
   - 密钥生成和存储
   - 密钥访问控制
   - 密钥更新机制

#### 2.3 测试脚本结构设计

##### 2.3.1 目录结构
```
test/
├── sapphire/                    # Sapphire网络专用测试
│   ├── contracts/              # 合约测试
│   │   ├── TruthBox.spec.ts
│   │   ├── Exchange.spec.ts
│   │   ├── FundManager.spec.ts
│   │   ├── TruthNFT.spec.ts
│   │   ├── UserId.spec.ts
│   │   └── AddressManager.spec.ts
│   ├── auth/                   # 认证测试
│   │   ├── SiweAuth.spec.ts
│   │   └── MultiDomain.spec.ts
│   ├── privacy/                # 隐私功能测试
│   │   ├── SapphireEncryption.spec.ts
│   │   └── PrivacyProtection.spec.ts
│   └── integration/            # 集成测试
│       ├── FullWorkflow.spec.ts
│       └── CrossContract.spec.ts
├── fixtures/                   # 测试固件
│   ├── sapphireFixture.ts
│   ├── authFixture.ts
│   └── privacyFixture.ts
└── utils/                      # 工具函数（扩展现有）
    ├── sapphireUtils.ts
    ├── authUtils.ts
    └── privacyUtils.ts
```

##### 2.3.2 测试固件设计
1. **sapphireFixture.ts** - Sapphire网络测试环境
   - 网络配置和切换
   - 合约部署和初始化
   - 测试账户管理

2. **authFixture.ts** - SIWE认证测试环境
   - SIWE消息生成
   - 令牌管理
   - 认证状态管理

3. **privacyFixture.ts** - 隐私功能测试环境
   - 加密数据生成
   - 密钥管理
   - 隐私验证工具

#### 2.4 测试用例设计原则

##### 2.4.1 模块化设计
- 参考`test-EVM/`的模块化结构
- 使用`test/utils/`中的工具函数
- 保持测试用例的独立性和可重用性

##### 2.4.2 认证集成
- 所有需要权限的操作都必须通过SIWE认证
- 测试不同角色的权限控制
- 验证认证令牌的有效性和安全性

##### 2.4.3 隐私保护验证
- 验证敏感数据的加密存储
- 测试数据访问的权限控制
- 确保隐私功能的正确性

##### 2.4.4 错误处理测试
- 测试各种异常情况
- 验证错误消息的准确性
- 确保合约的健壮性

#### 2.5 测试数据管理

##### 2.5.1 测试账户
- 使用`test/utils/getAccount.ts`中的账户管理
- 支持多角色测试（admin, minter, buyer, seller等）
- 确保账户权限的正确分配

##### 2.5.2 测试数据
- 使用`test/utils/common.ts`中的通用数据
- 创建Sapphire特定的测试数据
- 支持加密数据的测试

#### 2.6 网络环境配置

##### 2.6.1 测试网络
- **sapphire_localnet**: 本地测试网络
- **sapphire-testnet**: Sapphire测试网络
- 支持网络切换和配置管理

##### 2.6.2 部署配置
- 使用ignition部署系统
- 支持合约升级和配置管理
- 确保测试环境的一致性

### 3. 开发优先级

#### 3.1 第一阶段：基础功能测试
1. SiweAuth认证系统测试
2. 核心合约基本功能测试
3. 网络环境和部署配置

#### 3.2 第二阶段：隐私功能测试
1. Sapphire加密功能测试
2. 隐私保护机制验证
3. 密钥管理测试

#### 3.3 第三阶段：集成测试
1. 完整业务流程测试
2. 跨合约交互测试
3. 性能和压力测试

### 4. 质量保证

#### 4.1 测试覆盖率
- 目标：90%以上的代码覆盖率
- 重点：核心业务逻辑和隐私功能
- 确保所有关键路径都被测试

#### 4.2 测试稳定性
- 使用固定的测试数据
- 避免测试间的相互影响
- 确保测试结果的可重现性

#### 4.3 文档和注释
- 所有测试用例都有详细的中文注释
- 提供测试用例的说明文档
- 记录测试环境配置和部署步骤

### 5. 交付物

#### 5.1 测试脚本
- 完整的TypeScript测试套件
- 模块化的测试固件
- 工具函数库

#### 5.2 文档
- 测试用例说明文档
- 部署和运行指南
- 故障排除指南

#### 5.3 配置
- 网络配置文件
- 部署脚本
- 环境变量配置

<!-- 请从这里编写 -->
